---
layout: body
js: index.js
contacts:
    -   url: mailto:stephen.zappia@gmail.com
        icon: fas fa-envelope
    -   url: https://www.linkedin.com/in/stephen-zappia/
        icon: fab fa-linkedin
---
<div x-data="{ pageLoaded: false }"
    x-init="$nextTick(() => { pageLoaded = true })"
    class="mt-20 lg:mt-60 h-full w-full flex flex-col items-center justify-center"
    >
    {# Intro #}

    <h1 class="text-center"
        {% set delayCounter = 1 %}{% include "partials/delayed-fade-in.njk" %}
        >Hey, I'm Stephen.</h1>

    <h2 class="text-center"
        {% set delayCounter = delayCounter + 3 %}{% include "partials/delayed-fade-in.njk" %}
        >Nice to meet you.</h2>

    {# Projects #}

    {# Contact #}
    <div class="lg:mt-40 mt-20 text-center"
        {% set delayCounter = delayCounter + 8 %}{% include "partials/delayed-fade-in.njk" %}
        >
        <h5>Shall we?</h5>
        <div class="mt-4 flex items-center justify-around">
            {% for contact in contacts %}
                <a href="{{ contact.url }}" target="_blank" x-data="{ hover: false }" @mouseover="hover = !hover" @mouseout="hover = !hover" >
                    <i class="{{ contact.icon }} text-3xl transition-all" :class="{ 'text-regularBlue': hover }"></i>
                </a>
            {% endfor %}
        </div>
    </div>
</div>

<div class="h-80"></div>

{# Modals #}
{# <div x-data>
    {% set modalID = "faq" %}
    {% include "partials/modal.njk" %}
    {% set modalID = "help" %}
    {% include "partials/modal.njk" %}
</div> #}

{# <div x-data="componentData" class="text-center m-20">
    <span x-text="message"></span>
    {% set tooltipText = "This is a tooltip" %}
    {% include "partials/tooltip.njk" %}
</div> #}
